import { ShowOnFocusDirective } from './show-on-focus.directive';

describe('ShowOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
